fun fib <- n begin
    return ( n <= 1 ? 1 : fib(n-1) + fib(n-2) )
end

var a = clock()

for var i = 0; i < 25; i = i + 1 begin
    println fib(i)
end

var b = clock()

println b - a // around 6 seconds in the optimized interpreter